# intro-to-github
This is my first GitHub repository .
<br>
Author- Riddhi
<br>

Configuring Git- <br>
git config --global user.name " " <br>
git config --global email.user "" <br>
git config --list <br>
git clone CtrlV (paste the url of the repo u want on ur local system) <br> 
cd - change directory..it's a cmd  to change the directory ie folder <br>
cd (name of the repo here) <br>
Syntax - cd name of the folder enter<br>
ls cmd- to list files in the folder <br>
ls -a : to see hidden files also <br>

git status - types of status - staged (ready to be commited, after add stage), untracked, unmodified, modified
<hr>

Now for git add - git add (file name) or git add . -> ie to add all the files<br> 
git commit -m "Type a msg..meaningful like solved bugs, added a button etc etc" <br>

PUSH CMD- upload local repo to remote repo <br>
Type on terminal - git push origin main <br>
after typing this smd, vs code will redirect to github, then refresh n changes on github 


